import type IFilter from './IFilter';
import type IRunningEnvironment from '../IRunningEnvironment';
export default class FilterGitignore implements IFilter {
    useGitignore: boolean;
    absolutePath: string;
    gitIgnore: {
        accepts(input: string): boolean;
        denies(input: string): boolean;
        maybe(input: string): boolean;
    };
    runningEnvironment: IRunningEnvironment;
    constructor(useGitignore: boolean, absolutePath: string, runningEnvironment: IRunningEnvironment);
    matchFilter(path: string): Promise<boolean>;
    private getGitIgnoreOrCreate;
    private getRelativeMatchPath;
    private removeAbsolutePath;
    static configureGitignore(useGitignore: boolean, absolutePath: string, runningEnvironment: IRunningEnvironment): Promise<{
        accepts(input: string): boolean;
        denies(input: string): boolean;
        maybe(input: string): boolean;
    }>;
}
