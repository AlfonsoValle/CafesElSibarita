import type IFilter from "../IFilter";
export default class FilterCollection {
    filters: IFilter[];
    constructor(filters?: IFilter[]);
    addFilter(filter: IFilter): void;
    matchFilters(path: string, deep: number): Promise<boolean>;
}
