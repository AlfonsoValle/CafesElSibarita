import type ICharset from '../charset/ICharset';
export default class TextDirectoryRepresentation {
    charset: ICharset;
    constructor(charset: ICharset);
    directoryNotEmpty(previousContent?: string): string;
    previousContent(isLast: boolean): string;
    directoryItem(isLast: boolean, previousContent?: string, itemName?: string, isDirectory?: boolean): string;
}
