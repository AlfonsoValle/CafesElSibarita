import { FilterCollection } from '../../filters';
import { DirectoryNode } from '../';
import type IRunningEnvironment from '../../IRunningEnvironment';
export default class DirectoryParser {
    directoryPath: string;
    filters: FilterCollection;
    maxLevel: number;
    markNoEmptyDirectories: boolean;
    runningEnvironment: IRunningEnvironment;
    constructor(directoryPath: string, filters: FilterCollection, runningEnvironment: IRunningEnvironment, maxLevel?: number | null, markNoEmptyDirectories?: boolean);
    parse(): Promise<DirectoryNode>;
    includeChildrenFromDirectories(parent: DirectoryNode, currentDirectory: string, currentLevel: number): Promise<void>;
}
