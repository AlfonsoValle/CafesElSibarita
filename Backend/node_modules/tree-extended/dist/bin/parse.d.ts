import Configuration from '../Configuration';
export declare function isHelp(args: string[]): boolean;
declare function getConfigurationAndPathFromArguments(args: string[]): {
    path: string;
    configuration: Configuration;
};
export default getConfigurationAndPathFromArguments;
